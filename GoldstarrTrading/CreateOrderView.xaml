<Page
    x:Class="GoldstarrTrading.CreateOrderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoldstarrTrading"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="400*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Create order" Grid.Column="1" Grid.Row="1" FontSize="38"/>
        <TextBlock Text="Order history" Grid.Column="2" Grid.Row="1" FontSize="38"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="70" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Customer" FontSize="32" Grid.Row="2" VerticalAlignment="Center" />
            <DropDownButton x:Name="CustomerDropDown" Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Width="auto"  />

            <TextBlock Text="Merchandise" FontSize="32" Grid.Row="3" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{x:Bind vm.ObsMerch}" x:Name="Combo" Grid.Row="3" Grid.Column="2" VerticalAlignment="Center" PlaceholderText="Merchandise">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ProductName}" ></TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>

            </ComboBox>

            <TextBlock Text="Amount" FontSize="32" Grid.Row="4" VerticalAlignment="Center"/>
            <DropDownButton x:Name="AmountDropDown" Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" Width="auto" />
            

        </Grid>
    </Grid>
</Page>
