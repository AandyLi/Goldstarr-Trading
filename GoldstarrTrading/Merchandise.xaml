<Page
    x:Class="GoldstarrTrading.Merchandise"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoldstarrTrading"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <ListView x:Name="merchandiseList" Grid.Row ="1" Margin="140,163,417,59">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Produktnamn:" FontSize="20"/>
                        <TextBlock Text="{Binding ProductName}" FontSize="20" />
                        <TextBlock Text="Leverantör:" FontSize="20"/>
                        <TextBlock Text="{Binding Supplier}" FontSize="16"/>
                        <TextBlock Text="Antal i lager:" FontSize="20"/>
                        <TextBlock Text="{Binding Amount}" FontSize="16"/>
                    </StackPanel>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
